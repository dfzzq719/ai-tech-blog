# AI Tech Blog - 全自动多语言AI技术博客

基于 Docusaurus 构建的全自动多语言AI技术博客系统。

## 🌐 访问地址

- 生产环境：http://101.42.11.124:8001
- 语言：English | 中文 | 日本語

## 🚀 快速开始

### 本地开发

```bash
# 安装依赖
npm install

# 启动开发服务器
npm start

# 构建
npm run build

# 本地预览构建结果
npm run serve
```

### Docker 部署

```bash
# 构建并启动
docker-compose up -d --build

# 查看日志
docker-compose logs -f

# 停止
docker-compose down
```

## 📁 项目结构

```
aiblog-TencentCloud/
├── blog/                    # 博客文章 (英文原版)
│   └── YYYY-MM-DD-slug/
│       └── index.md
├── i18n/                    # 多语言翻译
│   ├── zh/                  # 中文
│   │   └── docusaurus-plugin-content-blog/
│   └── ja/                  # 日文
│       └── docusaurus-plugin-content-blog/
├── static/
│   └── audio/               # TTS音频文件
├── src/
│   ├── css/
│   └── pages/
├── automation/              # 自动化脚本
│   ├── collector/           # RSS采集器
│   ├── processor/           # LLM内容处理
│   ├── translator/          # 翻译 + TTS
│   ├── publisher/           # 文章发布
│   ├── config.py            # 配置
│   └── main.py              # 主入口
├── docusaurus.config.js     # Docusaurus配置
├── Dockerfile
├── docker-compose.yml
└── nginx.conf
```

## 🔧 自动化流程

```
┌──────────────────────────────────────────────────────────────────┐
│  RSS源 → 采集器 → AI加工 → 翻译 → TTS → 发布                      │
│                                                                  │
│  MIT Tech Review ─┐                                              │
│  AI Weekly ───────┼──→ RSSCollector → LLMProcessor              │
│  OpenAI Blog ─────┤         ↓              ↓                     │
│  Google AI ───────┤    去重队列      专业风格重写                  │
│  DeepMind ────────┘         ↓              ↓                     │
│                         Article     ProcessedArticle             │
│                                        ↓                         │
│                              TranslationPipeline                 │
│                              ├── DeepL 翻译                      │
│                              └── Edge TTS 音频                   │
│                                        ↓                         │
│                              ArticlePublisher                    │
│                              └── 写入 blog/ + i18n/              │
└──────────────────────────────────────────────────────────────────┘
```

## 🤖 使用自动化系统

### 1. 配置 API Keys

```bash
cd automation
cp .env.example .env
# 编辑 .env 填入你的 API Keys
```

### 2. 安装 Python 依赖

```bash
pip install -r requirements.txt
```

### 3. 运行流水线

```bash
# 完整流水线
python main.py

# 仅采集文章
python main.py --collect

# 限制文章数量
python main.py --max 3

# 跳过音频生成
python main.py --skip-audio
```

## 📝 手动添加文章

1. 在 `blog/` 下创建 `YYYY-MM-DD-slug/index.md`
2. 在 `i18n/zh/` 和 `i18n/ja/` 下创建对应翻译
3. 在 `static/audio/YYYY-MM-DD-slug/` 添加音频文件

## 🛠 技术栈

| 模块 | 技术 |
|------|------|
| 静态站点 | Docusaurus 3.x |
| 容器化 | Docker + nginx |
| 内容采集 | Python + feedparser |
| AI处理 | GPT-4o-mini |
| 翻译 | DeepL API |
| TTS | Edge TTS |
| CI/CD | GitHub Actions |

## 💰 成本估算

| 项目 | 月成本 |
|------|--------|
| GPT-4o-mini | ¥50-150 |
| DeepL Free | ¥0 |
| Edge TTS | ¥0 |
| 服务器 | 已有 |
| **合计** | **¥50-150/月** |

## 📄 License

MIT
